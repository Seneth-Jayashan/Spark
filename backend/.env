# Server
PORT=3001
NODE_ENV=development

CORS_ORIGIN=http://localhost:3000

# Rate limiting
RATE_LIMIT_MAX=300

# MongoDB
MONGO_URI=mongodb+srv://admin_sparkVMS:SSAT.Spark2025@sparkvms.ywyryqv.mongodb.net/?retryWrites=true&w=majority&appName=SparkVms
MONGO_DB=spark_vms
MONGO_MAX_POOL=20

# Auth (when you add JWT)
JWT_SECRET=SSAT@Spark2025
JWT_EXPIRES_IN=7d

# Email / SMTP config
SMTP_HOST=smtp.mailtrap.io       
SMTP_PORT=587
SMTP_SECURE=false                
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

FROM_EMAIL=noreply@spark-vms.org

# Frontend URL (for links)
CLIENT_URL=http://localhost:3000
FRONTEND_URL = http://localhost:5173
